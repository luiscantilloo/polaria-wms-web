# URL del backend (polaria-wms-api). En local suele ser el puerto 3000.
# El frontend (Next.js) corre en otro puerto (ej. 3001) y proxea /api/* hacia aquí.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
